This project is a back-up containing all the classes that have been added to yoink.
Therefor, it depends on yoink-core-bootstrap.jar file, which needs to be added into the /libs folder.
The classes are inside their corresponding folder (i.e. interfaces are inside /api).
This project can be and was used to create yoink-core-database module inside Yoink.
